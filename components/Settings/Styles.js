import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    content: {
        padding: 10
    },
    first: {
        marginTop: 10
    },
    last: {
        marginBottom: 10
    }
});